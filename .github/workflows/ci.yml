name: CI Pipeline

on: [push]

jobs:
  call-central-pipeline:
    # REPLACE [YOUR-USERNAME] below with your actual GitHub username!
    uses: mlochman/devops-governance/.github/workflows/standard-build.yml@main
    with:
      environment: 'production'
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
